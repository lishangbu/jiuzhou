/**
 * 近期活跃角色筛选脚本桥接模块
 *
 * 作用（做什么 / 不做什么）：
 * 1. 做什么：为脚本侧保留原有导入路径，同时统一转发到运行时共享实现。
 * 2. 做什么：确保脚本与服务共用同一份活跃角色筛选口径，避免继续复制 SQL。
 * 3. 不做什么：不新增脚本私有逻辑，也不覆盖共享实现的参数校验。
 *
 * 输入/输出：
 * - 输入：与 `services/shared/recentActiveCharacterSelector` 完全一致。
 * - 输出：直接复用共享模块输出。
 *
 * 数据流/状态流：
 * 脚本调用 -> 本桥接文件 -> 运行时共享模块 -> 返回统一活跃角色结果。
 *
 * 复用设计说明：
 * 1. 现有脚本仍可保持旧路径不动，运行时云游改动也能直接复用同一入口。
 * 2. 新增字段或筛选规则只改共享模块一处，脚本桥接不再复制实现。
 *
 * 关键边界条件与坑点：
 * 1. 这里只做转发，不能在这里加额外字段映射，否则脚本与运行时会再次分叉。
 * 2. 导出名必须保持不变，避免现有脚本导入路径稳定但符号缺失。
 */

export * from '../../services/shared/recentActiveCharacterSelector.js';
