/**
 * 伙伴回收脚本生成时间截止线
 *
 * 作用（做什么 / 不做什么）：
 * 1. 做什么：集中定义伙伴回收脚本使用的“生成时间截止线”，避免 SQL 过滤、控制台摘要、测试各自手写一份日期常量。
 * 2. 做什么：明确把业务口径固定为中国时区时间点，后续若截止时间调整，只需要改这一处。
 * 3. 不做什么：不负责数据库查询、不负责格式化任意时间，也不推导动态时间窗口。
 *
 * 输入/输出：
 * - 输入：无。
 * - 输出：导出固定截止时间 `PARTNER_RECLAIM_GENERATION_CUTOFF_AT` 与对应展示文案 `PARTNER_RECLAIM_GENERATION_CUTOFF_LABEL`。
 *
 * 数据流/状态流：
 * 脚本查询条件/控制台摘要/测试 -> 复用本模块常量 -> 统一按同一截止线过滤与展示。
 *
 * 关键边界条件与坑点：
 * 1. 这里使用的是固定中国时区时间点，不应再在调用方额外做一次时区偏移；否则会把 2026-03-23 18:00:00 误算成其他时间。
 * 2. 业务要求是“18:00 前”，因此等于 18:00:00 的记录不应命中；比较时必须使用严格小于号。
 */

export const PARTNER_RECLAIM_GENERATION_CUTOFF_AT = new Date('2026-03-23T18:00:00+08:00');
export const PARTNER_RECLAIM_GENERATION_CUTOFF_LABEL = '中国时区 2026-03-23 18:00:00 前';
